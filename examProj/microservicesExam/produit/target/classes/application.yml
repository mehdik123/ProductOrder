server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:produitdb;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
